type Consumables = {
    days: number;
    weeks: number;
    months: number;
    years: number;
    fullString: string;
};

export type ConsumablesKeys = 'days' | 'weeks' | 'months' | 'years';

export default Consumables;
